import './Settings.css';

const Tutorial = ({ socket }) => {

    return (
        <div className={'settings'}>
            <button>
                ⚙
            </button>
        </div>
    )
}

export default Tutorial;
